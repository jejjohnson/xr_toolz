"""Generic xarray geoprocessing — domain-agnostic operators.

This submodule hosts everything that applies across Earth-science
domains: coordinate validation, spatial/temporal subsetting, masks,
regridding, detrending (climatology, anomalies), interpolation, metrics,
spectral analysis, encoders, CRS utilities, sklearn interop, and the
``ModelOp`` inference wrappers.

Content lands incrementally over upcoming releases. See
``docs/design/`` for the full scope and roadmap.
"""

__all__: list[str] = []
